import React from 'react';import {PlayerProfile} from '../types';
export function Profile({profile,onBack}:{profile:PlayerProfile;onBack:()=>void}){const completed=profile.completedLevels.length;const stars=Object.values(profile.starsByLevel).reduce((a,b)=>a+b,0);const score=Math.max(0,...Object.values(profile.bestScores),0);return <section className="panel profile"><button className="back" onClick={onBack}>← VOLVER</button><div className="avatar">{profile.username.slice(0,1).toUpperCase()||'✦'}</div><h1>{profile.username}</h1><p className="eyebrow">GUARDIÁN DE LA BIBLIOTECA</p><div className="stat-grid"><div><strong>NIVEL {profile.currentLevel}</strong><Progress value={completed/30*100}/><span>{Math.round(completed/30*100)}% COMPLETADO</span></div><div><strong>ESTRELLAS</strong><em>{stars} / 90</em></div><div><strong>MEJOR PUNTUACIÓN</strong><em>{score.toLocaleString()}</em></div><div><strong>PARTIDAS</strong><em>{profile.totalGames}</em></div><div><strong>PAREJAS ENCONTRADAS</strong><em>{profile.totalMatches}</em></div><div><strong>LOGROS</strong><em>{profile.unlockedAchievements.length} / 15</em></div></div></section>}
export function Progress({value}:{value:number}){return <div className="progress"><i style={{width:`${Math.min(100,value)}%`}}/></div>}
