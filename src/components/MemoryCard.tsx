import React from 'react';import {Card} from '../types';
export function MemoryCard({card,onClick,disabled}:{card:Card;onClick:()=>void;disabled?:boolean}){return <button className={`memory-card ${card.flipped||card.matched?'is-open':''} ${card.matched?'is-matched':''}`} onClick={onClick} disabled={disabled||card.flipped||card.matched} aria-label={card.flipped?'Carta revelada':'Carta de recuerdo'}><span className="card-face card-back">✦<small>RUNE ARCANA</small>✦</span><span className="card-face card-front">{card.symbol}<small>RECUERDO {String(card.pairId+1).padStart(2,'0')}</small></span></button>}
