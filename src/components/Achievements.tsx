import React from 'react';import {Achievement,PlayerProfile} from '../types';
export function Achievements({profile,items,onBack}:{profile:PlayerProfile;items:Achievement[];onBack:()=>void}){return <section className="panel"><button className="back" onClick={onBack}>← VOLVER</button><p className="eyebrow">ARCHIVO DE HONORES</p><h1>Logros</h1><div className="achievement-grid">{items.map(a=><div className={`achievement ${profile.unlockedAchievements.includes(a.id)?'unlocked':''}`} key={a.id}><span>{a.icon}</span><div><b>{a.title}</b><small>{a.description}</small></div></div>)}</div></section>}
