import React from 'react';import {Level} from '../types';
export function LevelMap({levels,current,completed,stars,onSelect}:{levels:Level[];current:number;completed:number[];stars:Record<number,number>;onSelect:(id:number)=>void}){return <div className="level-map">{levels.map(l=>{const locked=l.id>current;const done=completed.includes(l.id);return <button key={l.id} disabled={locked} onClick={()=>onSelect(l.id)} className={`level-node ${locked?'locked':''} ${done?'completed':''} ${l.id===current?'available':''}`}><span>{done&&stars[l.id]===3?'♛':l.id}</span><small>{locked?'BLOQUEADO':done?'COMPLETADO':l.id===30?'FINAL':'DISPONIBLE'}</small>{done&&<b>{'★'.repeat(stars[l.id]||1)}</b>}</button>})}</div>}
